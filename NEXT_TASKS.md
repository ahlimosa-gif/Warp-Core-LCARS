# NEXT_TASKS.md

## Current Priority

1. Completed: `package.json` and project memory files are on GitHub `main`.
2. Completed: `package.json`, `AGENTS.md`, and `NEXT_TASKS.md` were read back from GitHub for verification.

## Suggested Follow-Ups

- Clean `warpcore.cmd` so it contains only executable REXX content.
- Update `README.md` with the modern Node.js/npm install path.
- Add an actual npm publish checklist if the package should be released to the registry.
- Add a smoke test script if more behavior is added beyond terminal rendering.
